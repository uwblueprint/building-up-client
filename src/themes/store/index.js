import { extendTheme } from '@chakra-ui/react';

const storeTheme = extendTheme({
  colors: {
    brand: {
      primary: '#CD1000',
      secondary: '#B20E00',
    },
  },
});

export default storeTheme;
